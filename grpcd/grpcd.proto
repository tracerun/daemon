syntax = "proto3";

message Action {
  string file_path = 1;
  int32 type = 2;
  int64 timestamp = 3;
}

message Empty {
}

// The service to receive actions.
service ActionService {
  rpc SendAction (Action) returns (Empty);
}