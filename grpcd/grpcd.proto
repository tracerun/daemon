syntax = "proto3";

message Action {
  string target = 1;
  uint32 active = 2;
}

message Empty {
}

// The service to receive actions.
service ActionService {
  rpc SendAction (Action) returns (Empty);
}